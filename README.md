# Chess Affiliate Multi-site MDX Starter

## Quick start

```bash
npm i
npm run dev
# open http://localhost:3000/cz/cs
```

Add MDX under `content/cz/cs/blog/...`

Deploy to Vercel, set `REVALIDATE_SECRET`, keep `vercel.json` with crons.

Fill in generator / publisher logic.
